server {
    listen 80;
    server_name i12a307.p.ssafy.io;  # 도메인 설정

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri /index.html;
    }

    location /_next {
        root /usr/share/nginx/html;
    }

    error_page 404 /index.html;
}
