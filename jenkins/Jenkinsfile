pipeline {
    agent any
    environment {
        ENV_FILE = credentials('project-env')
    }
    stages {
        stage('Setup Environment') {
            steps {
                withCredentials([file(credentialsId: 'project-env', variable: 'ENV_FILE')]) {
                    sh '''
                    cp $ENV_FILE ./backend/.env
                    cp $ENV_FILE ./frontend/.env
                    '''
                }
            }
        }
        stage('Backend Build') {
            steps {
                dir('backend') {
                    sh 'chmod +x ./gradlew'
                    sh './gradlew clean build'
                }
            }
        }
        stage('Frontend Build') {
            tools {
                nodejs "NodeJS-20"
            }
            steps {
                dir('frontend') {
                    sh '''
                    npm install
                    npm run build
                    '''
                }
            }
        }
        stage('Docker Compose') {
            steps {
                script {
                    sh '''
                    echo "Stopping and removing old containers if exist..."
                    docker-compose down || true

                    echo "Starting services with Docker Compose..."
                    docker-compose up -d --build

                    echo "Currently running containers and their ports:"
                    docker ps --filter "name=backend" --format "Backend running on port: {{.Ports}}"
                    docker ps --filter "name=frontend" --format "Frontend running on port: {{.Ports}}"
                    '''
                }
            }
        }
    }
}
